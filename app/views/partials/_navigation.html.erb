<!-- Navigation -->
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <%= link_to 'SPORTEUR', root_path, class: 'navbar-brand'%>
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav horizontal">
                    <li>
                        <a href="#">MATCHS</a>
                    </li>
                    <li>
                        <a href="#">Services</a>
                    </li>
                    <li>
                        <a href="#">Contact</a>
                    </li>

                    <% if user_signed_in? %>
                      <li class="col-md">
                        <%= link_to edit_user_registration_path do %>
                          <i class="material-icons be-icon">face</i> Profile
                        <% end %>
                      </li>
                      <li class="col-md">
                        <%= link_to destroy_user_session_path, method: :delete do %>
                          <i class="material-icons be-icon">power_settings_new</i> Sign Out
                        <% end %>
                      </li>
                    <% else %>
                      <li class="col-md">
                        <%= link_to new_user_session_path do %>
                          <i class="material-icons be-icon">account_circle</i> Sign In
                        <% end %>
                      </li>
                      <li class="col-md">
                        <%= link_to new_user_registration_path do %>
                          <i class="material-icons be-icon">account_box</i> Sign Up
                        <% end %>
                      </li>


                      
                      
                    <% end %>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container -->
    </nav>